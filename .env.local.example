MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=promptinghub
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace-me-with-openssl-rand-base64-32
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Optional / feature-gated — safe to leave blank
NEXT_PUBLIC_SITE_URL=http://localhost:3000
VERIFIED_HANDLES=
# X/Twitter daily ingest (official API only). Without a token the source is a no-op.
TWITTER_BEARER_TOKEN=
INGEST_QUERY=ai prompt
# Protects /api/cron/* (Vercel Cron sends `Authorization: Bearer <CRON_SECRET>`)
CRON_SECRET=
